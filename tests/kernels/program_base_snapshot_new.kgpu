# PROGRAM_BASE snapshot test new program.
# This program is intentionally loaded after NEW_PROGRAM_BASE. It must not run
# after PROGRAM_BASE is rewritten while the old program is active.

MOVSR R3, LINEAR_GLOBAL_ID
MOVI  R4, 2
MUL   R3, R3, R4
MOVI  R5, 0xA0
ADD   R5, R5, R3
MOVI  R6, 0xBEEF
STORE16 [R5 + 0], R6
END
